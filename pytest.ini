[pytest]
testpaths = tests
markers =
    integration: hits a live GenLayer network, real web and LLM calls

# Integration tests are opt-in so that `pytest tests/` stays fast and offline.
# A `-m` on the command line overrides this one.
addopts = -m "not integration"
